# GraphhopSimhash 量化配置说明

本文档说明当前 `GraphhopSimhash` 这一版的量化 embedding 生成方式，以及它如何和 hash reuse 联合实验配合使用。

当前版本已经把 **官方 llm-awq 源码** 作为外接子项目接入：

```text
third_party/llm-awq/
```

这份源码来自你手动下载的：

```text
/home/qiumingzhi/Simhash-S/OneForAll/llm-awq-main.zip
```

我们只接入了 AWQ 量化核心代码、README 和 LICENSE，没有把 `tinychat` 里的图片、视频等大文件放进项目。

## 1. 三类执行路径

联合实验里会比较三类 embedding 执行路径：

```text
Reference / FP:
    参考路径。clean full-precision 对比用 FP16；AWQ-family 对比用 W4A16。

W4A8:
    中等成本路径。先走官方 AWQ W4 权重量化，再加 activation 8-bit 动态仿射量化。

W4A4:
    低成本激进路径。先走官方 AWQ W4 权重量化，再加 activation 4-bit 动态仿射量化。

Hash reuse:
    hash 命中的节点直接复用缓存 embedding，不再走 FP/W4A8/W4A4 计算。
```

所以 `reuse_real_quant` 的逻辑是：

```text
hash 命中节点:
    直接 reuse，成本记为 0。

hash 未命中节点:
    再根据 real-quant policy 选择 FP / W4A8 / W4A4。
```

## 2. 当前支持的生成配置

`generate_real_quant_pools.py` 里现在有三类配置。

第一类是 bitsandbytes / 原始模型路径：

```text
fp16:
    tag = FP16
    kind = bnb
    W16A16，全精度 reference pool。

int8:
    tag = INT8
    kind = bnb
    bitsandbytes load_in_8bit=True。

int4:
    tag = INT4
    kind = bnb
    bitsandbytes load_in_4bit=True，nf4，bfloat16 compute。
```

第二类是官方 AWQ 路径：

```text
W4A16:
    tag = W4A16
    kind = awq
    使用 third_party/llm-awq 官方源码做 W4 weight-only AWQ。
    activation 保持 16-bit。

W4A8:
    tag = W4A8
    kind = awq_act
    使用同一条官方 AWQ W4 权重量化路径。
    在线性层输入处追加 activation 8-bit 动态仿射 fake quant。

W4A4:
    tag = W4A4
    kind = awq_act
    使用同一条官方 AWQ W4 权重量化路径。
    在线性层输入处追加 activation 4-bit 动态仿射 fake quant。
```

注意：上游 `llm-awq` 原生主要适合 causal-LM 结构，例如 LLaMA/Qwen2/OPT/Bloom/MPT/Falcon/BigCode/NeoX。当前项目额外加了一个 GraphhopSimhash 本地 DistilBERT adapter，所以 `ST` 现在也可以生成官方 AWQ 路径的 `W4A16` pool。

这个 ST adapter 不是上游原生支持，而是在 vendored AWQ 里补了 DistilBERT 的 block 识别和局部 scale 规则。`BERT`、`e5` 还没有做对应 adapter。

第三类是本项目保留的 fake W/A quant 路径：

```text
W4A16_FAKE:
    tag = W4A16_FAKE
    kind = fake_wa
    旧版近似 AWQ-style fake W4A16，保留用于复现旧实验。

W4A8_FAKE:
    tag = W4A8_FAKE
    kind = fake_wa
    旧版近似 fake W4A8，保留用于复现旧实验。
    weight = 4-bit
    activation = 8-bit

W4A4_FAKE:
    tag = W4A4_FAKE
    kind = fake_wa
    旧版近似 fake W4A4，保留用于复现旧实验。
    weight = 4-bit
    activation = 4-bit
```

一句话总结：

```text
W4A16      = 官方 llm-awq 源码路径，支持 LLaMA 和本项目 DistilBERT/ST adapter。
W4A8/W4A4  = 官方 AWQ W4 权重路径 + activation 动态仿射 fake quant。
W4A16_FAKE = 旧的近似实现，给复现实验用。
FP16       = 真正全精度 reference pool。
W4A8_FAKE/W4A4_FAKE = 旧的近似 fake W/A quant pool。
```

## 3. 官方 AWQ W4A16 怎么跑

单独给 LLaMA-2-7B 生成 Cora 的官方 AWQ W4A16 pool：

```bash
cd /home/qiumingzhi/Simhash-S/OneForAll

python -m GraphhopSimhash.generate_real_quant_pools \
  --datasets cora \
  --llm_name llama2_7b \
  --configs W4A16 \
  --overwrite
```

默认会生成：

```text
cache_data/cora_llama2_7b_oracle_W4A16.pt
```

同时会缓存 AWQ search 结果：

```text
cache_data/awq/cora_llama2_7b_w4_g128_n128_s512.pt
```

下次如果不加 `--awq_overwrite_results`，会复用这个 AWQ search 结果，不再重新搜索 scale/clip。

常用 AWQ 参数：

```text
--awq_calib_samples 128:
    用 128 个图节点文本做 AWQ calibration。

--awq_seqlen 512:
    AWQ calibration token block 长度。

--awq_q_group_size 128:
    权重量化 group size。128 是 AWQ 常用设置。

--awq_no_zero_point:
    关闭 zero-point，默认是开启 zero-point。

--awq_disable_auto_scale:
    关闭 AWQ auto scale search。

--awq_disable_mse_clip:
    关闭 MSE clipping search。

--awq_overwrite_results:
    强制重新搜索 AWQ scale/clip，而不是读取 cache_data/awq 里的缓存。
```

官方 AWQ 这一条不是我们手写的 `FakeQuantLinear`。它会调用：

```text
third_party/llm-awq/awq/quantize/pre_quant.py
    run_awq
    apply_awq

third_party/llm-awq/awq/quantize/quantizer.py
    pseudo_quantize_model_weight
```

本项目做的事情主要是：

```text
1. 把 third_party/llm-awq 加到 sys.path。
2. 用 Cora/PubMed/Arxiv 的节点文本替代 AWQ 默认 pileval calibration 数据。
3. 跑官方 run_awq 搜索并应用 scale/clip；如果读取缓存结果，则跑 apply_awq。
4. 跑官方 pseudo_quantize_model_weight。
5. 编码全图文本并保存 embedding pool。
```

## 4. ST / DistilBERT 的推荐量化命令

ST 现在可以走官方 AWQ W4A16 adapter。单独生成 ST 的 Cora W4A16 pool：

```bash
cd /home/qiumingzhi/Simhash-S/OneForAll

python -m GraphhopSimhash.generate_real_quant_pools \
  --datasets cora \
  --llm_name ST \
  --configs W4A16 \
  --awq_calib_samples 16 \
  --awq_seqlen 128 \
  --overwrite
```

会生成：

```text
cache_data/cora_ST_oracle_W4A16.pt
```

ST adapter 默认会关闭 AWQ MSE clip，日志里会看到：

```text
[AWQ] DistilBERT adapter disables MSE clip by default to avoid large activation-clip memory spikes.
```

原因是 DistilBERT 的 activation-clip 搜索在当前机器上显存峰值较大，而且 ST 的 W4A16 主要先验证 weight-only AWQ 路径。需要强行打开 clip 时可以加：

```bash
--awq_force_mse_clip
```

但这可能触发 OOM。

如果要生成 ST 的同一套 AWQ-family 对比池，也就是 `W4A16/W4A8/W4A4` 都基于官方 AWQ W4 权重路径：

```bash
cd /home/qiumingzhi/Simhash-S/OneForAll

python -m GraphhopSimhash.generate_real_quant_pools \
  --datasets cora \
  --llm_name ST \
  --configs W4A16 W4A8 W4A4 \
  --overwrite
```

会生成：

```text
cache_data/cora_ST_oracle_W4A16.pt
cache_data/cora_ST_oracle_W4A8.pt
cache_data/cora_ST_oracle_W4A4.pt
```

这里的 `W4A8/W4A4` 已经不是旧版 fake W/A quant。它们会先复用官方 AWQ W4 search 结果，再在线性层输入处做 A8/A4 动态仿射 activation quant。

如果要用纯 FP16 作为 clean reference，也可以把 configs 改成：

```bash
--configs fp16 W4A8 W4A4
```

这会额外生成 `cache_data/cora_ST_oracle_FP16.pt`，但 `W4A8/W4A4` 仍然走当前 AWQ-based 路径。

如果确实要复现旧的近似 fake W/A 实现，可以显式用：

```bash
python -m GraphhopSimhash.generate_real_quant_pools \
  --datasets cora \
  --llm_name ST \
  --configs W4A16_FAKE W4A8_FAKE W4A4_FAKE \
  --overwrite
```

对应输出是：

```text
cache_data/cora_ST_oracle_W4A16_FAKE.pt
cache_data/cora_ST_oracle_W4A8_FAKE.pt
cache_data/cora_ST_oracle_W4A4_FAKE.pt
```

当前 ST adapter 的 scale 规则比较保守：由于 DistilBERT 是 post-norm，`sa_layer_norm` 输出还会进入 residual add，所以不能像 LLaMA 那样随便缩放 attention/FFN 前的 LayerNorm。现在只启用可以局部吸收的规则：

```text
attention.v_lin -> attention.out_lin
ffn.activation  -> ffn.lin2
```

`q_lin/k_lin/ffn.lin1` 仍会做 W4 pseudo quant，但不做 AWQ scale 吸收。

## 5. W4A8 / W4A4 的 AWQ-based activation quant 逻辑

`W4A8` 和 `W4A4` 当前先走官方 AWQ W4 权重量化，然后再把模型里的 `nn.Linear` 包成 `ActivationQuantLinear`。

也就是：

```text
W4A16:
    official AWQ W4 weights + FP16 activation

W4A8:
    official AWQ W4 weights + A8 activation fake quant

W4A4:
    official AWQ W4 weights + A4 activation fake quant
```

activation 量化发生在线性层输入处：

```text
Y = Linear(quantize_activation(X), W_awq4, b)
```

这里的 activation quant 使用动态仿射 min-max 量化，不再用对称量化：

```text
q_min = 0
q_max = 2 ** bits - 1

scale = (max(x) - min(x)) / q_max
zero_point = round(q_min - min(x) / scale)
x_q = (clamp(round(x / scale + zero_point), q_min, q_max) - zero_point) * scale
```

粒度是 per-token / per-row，也就是对 activation 最后一维单独取 min/max。这个比对称 A4 更适合 GELU/LayerNorm 后的非对称 activation 分布。

旧版 `W4A8_FAKE/W4A4_FAKE` 才走本项目里的 `FakeQuantLinear`：

每个 Linear 原始形式是：

```text
Y = X W^T + b
```

校准时先搜索一个输入通道 scale：

```text
s = sx^alpha * sw^(1 - alpha)
```

其中：

```text
sx:
    calibration activation 在输入通道上的 mean(abs(x))

sw:
    weight 在输入通道上的 max(abs(w))

alpha:
    在 0 到 1 之间搜索，默认搜索 21 个点
```

搜索时用局部 Linear 输出 MSE 选最好的 scale：

```text
y_orig  = Linear(x_eval, W)
y_quant = Linear(x_eval, dequant(quant(W * s)) / s)
loss    = mean((y_orig - y_quant)^2)
```

正式 forward 时：

```text
W_scaled = W * s
W_q      = symmetric_fake_quantize(W_scaled, weight_bits)

X_scaled = X / s
X_q      = symmetric_fake_quantize(X_scaled, activation_bits)

Y_q      = Linear(X_q, W_q, b)
```

fake quant 是对称量化：

```text
q_min = -(2 ** (bits - 1))
q_max =  (2 ** (bits - 1)) - 1

scale = max(abs(x)) / q_max
x_q   = clamp(round(x / scale), q_min, q_max) * scale
```

粒度是：

```text
weight:
    对 Linear weight 的每个 output row 单独取 scale。

activation:
    对 activation 的最后一维单独取 scale，近似 per-token / per-row dynamic activation quant。
```

## 6. Output affine alignment

`W4A16_FAKE/W4A8_FAKE/W4A4_FAKE` 这些旧 fake W/A pool 默认会做一次轻量输出对齐：

```text
E_fp ~= gamma * E_q + beta
```

其中：

```text
E_fp:
    FP16 reference embedding。

E_q:
    fake W/A quant 生成出来的 embedding。

gamma, beta:
    每个 embedding 维度单独拟合的一组 affine 参数。
```

拟合默认使用前 512 个节点：

```text
--ptq_align_samples 512
```

然后对全图 quantized embedding 做：

```text
E_aligned = normalize(gamma * E_q + beta)
```

这个步骤默认开启：

```text
--ptq_align_output
```

如果要关闭：

```bash
--no_ptq_align_output
```

注意：官方 AWQ `W4A16/W4A8/W4A4` 不走这个 output affine alignment。alignment 只对 `kind=fake_wa` 的旧配置生效。

## 7. 默认生成参数

当前 pool 生成脚本的默认参数是：

```text
--batch_size 64
--w4a_calib_samples 64
--ptq_align_samples 512
```

这两个 64 不要相乘理解：

```text
w4a_calib_samples = 64:
    一共拿 64 个文本节点做 calibration。

batch_size = 64:
    每次 forward 最多喂 64 个文本。
```

对于 Cora：

```text
节点总数 = 2708
calibration 文本数 = 64
calibration forward batch 数 = 1
全图 encoding batch 数 = ceil(2708 / 64) = 43
```

不是 `64 * 64 = 4096`。

## 8. 不包括 hash reuse 的统一量化对比命令

ST + Cora 的量化 policy 对比，不包括 hash reuse。现在如果要比较同一套 AWQ-family 路径，推荐用 `W4A16` 作为 reference：

```bash
cd /home/qiumingzhi/Simhash-S/OneForAll

python -m GraphhopSimhash \
  --datasets cora \
  --runs 1 \
  --experiment_suite real_quant_ablation \
  --real_quant_policy_suite w4a8_budget \
  --real_quant_model_name ST \
  --real_quant_fp_tag W4A16 \
  --real_quant_int8_tag W4A8 \
  --real_quant_int4_tag W4A4 \
  --real_quant_fp_ratio 0.0 \
  --real_quant_int8_ratio 0.20 \
  --real_quant_error_norm 1.0
```

这条命令会读：

```text
cache_data/cora_ST_oracle_W4A16.pt
cache_data/cora_ST_oracle_W4A8.pt
cache_data/cora_ST_oracle_W4A4.pt
```

如果这些文件不存在，先用第 4 节的 `generate_real_quant_pools` 命令生成。

如果要用真正 FP16 embedding 做 clean reference，把 `--real_quant_fp_tag W4A16` 改成 `--real_quant_fp_tag FP16` 即可。

## 9. hash reuse + real quant 联合命令

ST + Cora 的联合实验推荐用 AWQ-family reference：

```bash
cd /home/qiumingzhi/Simhash-S/OneForAll

python -m GraphhopSimhash \
  --datasets cora \
  --seed 44 \
  --runs 1 \
  --experiment_suite reuse_real_quant \
  --real_quant_policy_suite w4a8_budget \
  --real_quant_model_name ST \
  --real_quant_fp_tag W4A16 \
  --real_quant_int8_tag W4A8 \
  --real_quant_int4_tag W4A4 \
  --real_quant_fp_ratio 0.0 \
  --real_quant_int8_ratio 0.20 \
  --real_quant_error_norm 1.0 \
  --learned_hash_epochs 10 \
  --learned_hash_dim 128 \
  --hamming_only_acceptor \
  --enable_score_gate \
  --main_hash_head_bits 16 16 16 16 16 16 16 16 \
  --route_min_support_hits 3
```

`reuse_real_quant` 入口会自动覆盖生成三份 pool：

```text
cache_data/cora_ST_oracle_W4A16.pt
cache_data/cora_ST_oracle_W4A8.pt
cache_data/cora_ST_oracle_W4A4.pt
```

日志里会出现：

```text
[RealQuantAutoGen] Regenerating real-quant feature pools before reuse_real_quant; existing cache files will be overwritten.
[RealQuantAutoGen] FP: tag=W4A16 | config=W4A16 | batch_size=64 | w4a_calib_samples=64 | awq_calib_samples=16 | awq_seqlen=128
[RealQuantAutoGen] INT8: tag=W4A8 | config=W4A8 | batch_size=64 | w4a_calib_samples=64 | awq_calib_samples=16 | awq_seqlen=128
[RealQuantAutoGen] INT4: tag=W4A4 | config=W4A4 | batch_size=64 | w4a_calib_samples=64 | awq_calib_samples=16 | awq_seqlen=128
```

如果要用真正 FP16 embedding 做 clean reference，把 `--real_quant_fp_tag W4A16` 改成 `--real_quant_fp_tag FP16` 即可。

如果要用 LLaMA + 官方 AWQ W4A16 做 reference，把模型改成：

```bash
--real_quant_model_name llama2_7b \
--real_quant_fp_tag W4A16
```

这会在实验前自动生成：

```text
cache_data/cora_llama2_7b_oracle_W4A16.pt
cache_data/cora_llama2_7b_oracle_W4A8.pt
cache_data/cora_llama2_7b_oracle_W4A4.pt
```

其中 `W4A16` 走官方 AWQ weight-only，`W4A8/W4A4` 走同一套官方 AWQ W4 权重并追加 A8/A4 activation fake quant。

## 10. w4a8_budget 的预算含义

这组参数：

```bash
--real_quant_fp_ratio 0.0
--real_quant_int8_ratio 0.20
```

在 `reuse_real_quant` 里是对 hash miss 节点生效，不是对全图直接生效。

例如某次结果：

```text
Reuse = 44.7%
Miss  = 55.3%
```

那么：

```text
W4A8 全图比例 = 55.3% * 20% = 11.1%
W4A4 全图比例 = 55.3% * 80% = 44.2%
FP   全图比例 = 55.3% * 0%  = 0.0%
```

所以表里看到：

```text
W4A4 % = 44.2%
W4A8 % = 11.1%
FP %   = 0.0%
```

这是正常的，不是比例算错。

## 11. 表格里的几类 policy

`--real_quant_policy_suite w4a8_budget` 会输出这些配置：

```text
AllFP:
    hash miss 节点全部走 real_quant_fp_tag。
    AWQ-family 对比用 W4A16；clean full-precision reference 用 FP16。

UniformW4A8:
    hash miss 节点全部走 W4A8。

UniformW4A4:
    hash miss 节点全部走 W4A4。

RandomTopK_W4A8:
    在 hash miss 节点里随机挑 real_quant_int8_ratio 比例走 W4A8，其余走 W4A4。

DegreeTopK_W4A8:
    在 hash miss 节点里按 degree 重要性挑 TopK 走 W4A8，其余走 W4A4。

TSERTopK_W4A8:
    在 hash miss 节点里按 TSER / sensitivity 分数挑 TopK 走 W4A8，其余走 W4A4。

QuantTSERTopK_W4A8:
    使用量化版本的 TSER 分数挑 TopK 走 W4A8。

DegreeErrorTopK_W4A8:
    按 degree 重要性 * 实际 W4A4 embedding error 排序。

TSERErrorTopK_W4A8:
    按 TSER 重要性 * 实际 W4A4 embedding error 排序。
```

其中 `DegreeErrorTopK` 和 `TSERErrorTopK` 会用已经生成好的 W4A4/W4A8 pool 和 reference pool 计算 embedding cosine error。它们属于 oracle-style 分析，用来判断“如果知道实际量化误差，策略上限能到哪里”。

## 12. Cost 和百分比怎么读

联合实验里：

```text
Reuse %:
    hash 命中比例，直接复用，成本记为 0。

W4A4 %:
    hash miss 后选择 W4A4 的全图比例。

W4A8 %:
    hash miss 后选择 W4A8 的全图比例。

FP %:
    hash miss 后选择 real_quant_fp_tag 的全图比例。

Cost:
    相对计算成本。
```

成本公式：

```text
Cost = 0.00 * Reuse%
     + 0.25 * W4A4%
     + 0.50 * W4A8%
     + 1.00 * FP%
```

由于 reuse 成本记为 0，所以：

```text
Reuse % + W4A4 % + W4A8 % + FP % = 100%
```
