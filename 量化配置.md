# GraphhopSimhash 量化配置说明

本文档说明当前 `GraphhopSimhash` 这一版的量化 embedding 生成方式，以及它如何和 hash reuse 联合实验配合使用。

当前这版的核心目标不是单独证明某个量化 backend 最强，而是构造三条可比较的 embedding 执行路径：

```text
W4A16:
    高精度 / 保护路径。在当前命令里被放到 real_quant_fp_tag 位置。

W4A8:
    中等成本路径。比 W4A4 稳，成本按 0.50 计算。

W4A4:
    低成本激进路径。成本按 0.25 计算，但精度风险最大。

Hash reuse:
    命中的节点直接复用缓存 embedding，不再走 W4A16/W4A8/W4A4 计算。
```

因此 `reuse_real_quant` 的实验逻辑是：

```text
hash 命中节点:
    直接 reuse，成本记为 0。

hash 未命中节点:
    再根据 real-quant policy 选择 W4A16 / W4A8 / W4A4。
```

## 1. 当前版本和 PTQ_EMBEDDING_QUANT.md 的区别

`PTQ_EMBEDDING_QUANT.md` 描述的是更完整的 calibration-aware PTQ 设计，例如 group-wise quantization、percentile clipping、output alignment、outlier backend 等。

当前代码里的 `W4A16/W4A8/W4A4` 生成路径更轻量，主要实现是：

```text
FakeQuantLinear
+ symmetric fake quant
+ AWQ-style scale search
+ mean-pool normalized embedding
+ output affine alignment to FP16
```

主要代码位置：

```text
generate_real_quant_pools.py
    CONFIG_SPECS
    FakeQuantLinear
    symmetric_fake_quantize
    replace_linear_with_fake_quant
    generate_pool

real_quant.py
    regenerate_real_quant_pools
    load_real_quant_pools
    select_real_quant_policy_actions

runner.py
    run_reuse_real_quant_experiment
```

## 2. 支持的量化配置

`generate_real_quant_pools.py` 里当前有两类配置。

第一类是 bitsandbytes 路径：

```text
fp16:
    tag = FP16
    kind = bnb
    W16A16

int8:
    tag = INT8
    kind = bnb
    load_in_8bit=True

int4:
    tag = INT4
    kind = bnb
    load_in_4bit=True, nf4, bfloat16 compute
```

第二类是当前实验主要使用的 fake W/A quant 路径：

```text
W4A16:
    tag = W4A16
    kind = fake_wa
    weight = 4-bit
    activation = 16-bit

W4A8:
    tag = W4A8
    kind = fake_wa
    weight = 4-bit
    activation = 8-bit

W4A4:
    tag = W4A4
    kind = fake_wa
    weight = 4-bit
    activation = 4-bit
```

注意：在下面这条联合实验命令里，`real_quant_fp_tag` 被设置成 `W4A16`。

这意味着表格里的 `FP %` 列表示“保留路径”比例，但这个保留路径实际加载的是：

```text
cache_data/cora_ST_oracle_W4A16.pt
```

也就是说，当前命令里的 `AllFP` 不是原始 ST full precision pool，而是“所有 hash miss 节点走 W4A16 路径”。

## 3. W4A pool 是怎么生成的

以 `ST + Cora + W4A4` 为例，高层流程是：

```text
1. 加载 ST encoder。
2. 如果配置是 W4A16/W4A8/W4A4，则先按 fp16 方式加载原模型。
3. 把模型里的 nn.Linear 替换成 FakeQuantLinear。
4. 用 calibration texts 做一次 forward，触发每层 FakeQuantLinear 的 scale calibration。
5. 用校准后的 fake-quant Linear 编码全图文本。
6. 对 last hidden state 做 mean pooling。
7. 对 embedding 做 L2 normalize。
8. 如果 FP16 reference pool 存在，对 fake W/A pool 做 output affine alignment。
9. 保存到 cache_data/{dataset}_{model}_oracle_{tag}.pt。
```

输出文件名示例：

```text
cache_data/cora_ST_oracle_W4A16.pt
cache_data/cora_ST_oracle_W4A8.pt
cache_data/cora_ST_oracle_W4A4.pt
```

## 4. FakeQuantLinear 的量化细节

每个 Linear 层原始形式是：

```text
Y = X W^T + b
```

当前版本先搜索一个输入通道 scale：

```text
s = sx^alpha * sw^(1 - alpha)
```

其中：

```text
sx:
    calibration activation 在输入通道上的 mean(abs(x))

sw:
    weight 在输入通道上的 max(abs(w))

alpha:
    在 0 到 1 之间搜索，默认搜索 21 个点
```

搜索时用局部 Linear 输出 MSE 选最好的 scale：

```text
y_orig  = Linear(x_eval, W)
y_quant = Linear(x_eval, dequant(quant(W * s)) / s)
loss    = mean((y_orig - y_quant)^2)
```

正式 forward 时：

```text
W_scaled = W * s
W_q      = symmetric_fake_quantize(W_scaled, weight_bits)

X_scaled = X / s
X_q      = symmetric_fake_quantize(X_scaled, activation_bits)

Y_q      = Linear(X_q, W_q, b)
```

这里的 fake quant 是对称量化：

```text
q_min = -(2 ** (bits - 1))
q_max =  (2 ** (bits - 1)) - 1

scale = max(abs(x)) / q_max
x_q   = clamp(round(x / scale), q_min, q_max) * scale
```

具体粒度：

```text
weight:
    对 Linear weight 的每个 output row 单独取 scale。

activation:
    对 activation 的最后一维单独取 scale，近似 per-token / per-row dynamic activation quant。
```

## 5. Output affine alignment

直接把 `W4A8/W4A4` embedding 和真正 `FP16` embedding 比，会把 W4 权重量化造成的整体空间偏移也算进去。这个偏移会导致相对 FP16 的掉点明显变大。

当前版本默认对 fake W/A pool 做一个轻量输出对齐：

```text
E_fp ~= gamma * E_q + beta
```

其中：

```text
E_fp:
    FP16 reference embedding。

E_q:
    W4A16 / W4A8 / W4A4 生成出来的 quantized embedding。

gamma, beta:
    每个 embedding 维度单独拟合的一组 affine 参数。
```

拟合使用前 512 个节点：

```text
--ptq_align_samples 512
```

然后对全图 quantized embedding 做：

```text
E_aligned = normalize(gamma * E_q + beta)
```

这个步骤默认开启：

```text
--ptq_align_output
```

如果要复现未对齐的原始 fake quant pool，可以显式关闭：

```bash
--no_ptq_align_output
```

注意：alignment 需要已有 FP16 reference pool。默认 reference 路径是：

```text
cache_data/{dataset}_{model}_oracle_FP16.pt
```

所以推荐生成顺序是：

```bash
python -m GraphhopSimhash.generate_real_quant_pools \
  --datasets cora \
  --llm_name ST \
  --configs fp16 W4A8 W4A4 \
  --overwrite
```

这样 `fp16` 会先生成，后面的 `W4A8/W4A4` 会自动对齐到这个 FP16 reference。

## 6. 默认生成参数

当前版本已经把 W4A pool 生成脚本的默认参数改成：

```text
--batch_size 64
--w4a_calib_samples 64
--ptq_align_samples 512
```

所以现在单独生成 ST 的 Cora 三份 pool，可以直接跑：

```bash
python -m GraphhopSimhash.generate_real_quant_pools \
  --datasets cora \
  --llm_name ST \
  --configs W4A16 W4A8 W4A4
```

等价于：

```bash
python -m GraphhopSimhash.generate_real_quant_pools \
  --datasets cora \
  --llm_name ST \
  --configs W4A16 W4A8 W4A4 \
  --batch_size 64 \
  --w4a_calib_samples 64
```

单独运行 `generate_real_quant_pools` 时，如果目标文件已经存在，默认会跳过已有文件；需要手动覆盖时再加：

```bash
--overwrite
```

但 `reuse_real_quant` 内部的自动生成会强制 `overwrite=True`，每次实验前都会覆盖三份 pool。

这两个参数不要相乘理解。

```text
w4a_calib_samples = 64:
    一共拿 64 个文本节点做 calibration。

batch_size = 64:
    每次 forward 最多喂 64 个文本。
```

对于 Cora：

```text
节点总数 = 2708
calibration 文本数 = 64
calibration forward batch 数 = 1
全图 encoding batch 数 = ceil(2708 / 64) = 43
```

不是 `64 * 64 = 4096`。

当前脚本的 calibration texts 是数据集文本列表的前 64 个：

```text
texts[:w4a_calib_samples]
```

这个设置重要的原因是：`FakeQuantLinear` 是 lazy calibration，每层第一次 forward 时会根据当前 batch 的 activation 做 scale calibration。`batch_size=64 + w4a_calib_samples=64` 可以保证第一次 calibration forward 覆盖完整的 64 个 calibration texts。

## 7. reuse_real_quant 的自动覆盖生成

当前版本已经把 `reuse_real_quant` 改成：

```text
正式实验前，先自动重新生成 real quant pools，并覆盖已有 cache。
```

也就是说，下面这条命令不需要你手动提前生成：

```bash
python -m GraphhopSimhash \
  --datasets cora \
  --seed 44 \
  --runs 1 \
  --experiment_suite reuse_real_quant \
  --real_quant_policy_suite w4a8_budget \
  --real_quant_model_name ST \
  --real_quant_fp_tag W4A16 \
  --real_quant_int8_tag W4A8 \
  --real_quant_int4_tag W4A4 \
  --real_quant_fp_ratio 0.0 \
  --real_quant_int8_ratio 0.20 \
  --real_quant_error_norm 1.0 \
  --learned_hash_epochs 10 \
  --learned_hash_dim 128 \
  --hamming_only_acceptor \
  --enable_score_gate \
  --main_hash_head_bits 16 16 16 16 16 16 16 16 \
  --route_min_support_hits 3
```

它会先自动执行等价于下面的生成逻辑：

```text
生成并覆盖:
    cache_data/cora_ST_oracle_W4A16.pt
    cache_data/cora_ST_oracle_W4A8.pt
    cache_data/cora_ST_oracle_W4A4.pt

生成参数:
    batch_size = 64
    w4a_calib_samples = 64
    ptq_align_output = True
    ptq_align_samples = 512
    max_length = 500
    w4a_awq_grid = 21
    overwrite = True
```

日志里会出现：

```text
[RealQuantAutoGen] Regenerating FP/INT8/INT4 feature pools before reuse_real_quant; existing cache files will be overwritten.
[RealQuantAutoGen] FP: tag=W4A16 | config=W4A16 | batch_size=64 | w4a_calib_samples=64
[RealQuantAutoGen] INT8: tag=W4A8 | config=W4A8 | batch_size=64 | w4a_calib_samples=64
[RealQuantAutoGen] INT4: tag=W4A4 | config=W4A4 | batch_size=64 | w4a_calib_samples=64
```

注意：这个自动覆盖生成目前接在 `reuse_real_quant` 入口上。`real_quant_ablation` 仍然按原逻辑加载已有 cache，不会自动覆盖生成。

## 8. w4a8_budget 的预算含义

这组参数：

```bash
--real_quant_fp_ratio 0.0
--real_quant_int8_ratio 0.20
```

在 `reuse_real_quant` 里是对 hash miss 节点生效，不是对全图直接生效。

例如某次结果：

```text
Reuse = 44.7%
Miss  = 55.3%
```

那么：

```text
W4A8 全图比例 = 55.3% * 20% = 11.1%
W4A4 全图比例 = 55.3% * 80% = 44.2%
FP   全图比例 = 55.3% * 0%  = 0.0%
```

所以表里看到：

```text
W4A4 % = 44.2%
W4A8 % = 11.1%
FP %   = 0.0%
```

这是正常的，不是比例算错。

## 9. 表格里的几类 policy

`--real_quant_policy_suite w4a8_budget` 会输出这些配置：

```text
AllFP:
    hash miss 节点全部走 real_quant_fp_tag。
    在当前命令里就是 W4A16。

UniformW4A8:
    hash miss 节点全部走 W4A8。

UniformW4A4:
    hash miss 节点全部走 W4A4。

RandomTopK_W4A8:
    在 hash miss 节点里随机挑 real_quant_int8_ratio 比例走 W4A8，其余走 W4A4。

DegreeTopK_W4A8:
    在 hash miss 节点里按 degree 重要性挑 TopK 走 W4A8，其余走 W4A4。

TSERTopK_W4A8:
    在 hash miss 节点里按 TSER / sensitivity 分数挑 TopK 走 W4A8，其余走 W4A4。

QuantTSERTopK_W4A8:
    使用量化版本的 TSER 分数挑 TopK 走 W4A8。

DegreeErrorTopK_W4A8:
    按 degree 重要性 * 实际 W4A4 embedding error 排序。

TSERErrorTopK_W4A8:
    按 TSER 重要性 * 实际 W4A4 embedding error 排序。
```

其中 `DegreeErrorTopK` 和 `TSERErrorTopK` 会用已经生成好的 W4A4/W4A8 pool 和 W4A16 pool 计算 embedding cosine error。它们属于 oracle-style 分析，用来判断“如果知道实际量化误差，策略上限能到哪里”。

## 10. Cost 和百分比怎么读

联合实验里：

```text
Reuse %:
    hash 命中比例，直接复用，成本记为 0。

W4A4 %:
    hash miss 后选择 W4A4 的全图比例。

W4A8 %:
    hash miss 后选择 W4A8 的全图比例。

FP %:
    hash miss 后选择 real_quant_fp_tag 的全图比例。

Cost:
    相对计算成本。
```

成本公式：

```text
Cost = 0.00 * Reuse%
     + 0.25 * W4A4%
     + 0.50 * W4A8%
     + 1.00 * FP%
```

由于 reuse 成本记为 0，所以：

```text
Reuse % + W4A4 % + W4A8 % + FP % = 100%
```

## 11. 当前 ST + Cora 的 sanity check

当前版本用下面命令验证过：

```bash
python -m GraphhopSimhash \
  --datasets cora \
  --seed 44 \
  --runs 1 \
  --experiment_suite reuse_real_quant \
  --real_quant_policy_suite w4a8_budget \
  --real_quant_model_name ST \
  --real_quant_fp_tag FP16 \
  --real_quant_int8_tag W4A8 \
  --real_quant_int4_tag W4A4 \
  --real_quant_fp_ratio 0.0 \
  --real_quant_int8_ratio 0.20 \
  --real_quant_error_norm 1.0 \
  --learned_hash_epochs 10 \
  --learned_hash_dim 128 \
  --hamming_only_acceptor \
  --enable_score_gate \
  --main_hash_head_bits 16 16 16 16 16 16 16 16 \
  --route_min_support_hits 3
```

关键日志：

```text
[RealQuantError] INT8 mean=0.06913 | INT4 mean=0.20420
```

最终表格中的关键行：

```text
Baseline Acc: 0.6804

AllFP           W4A4=0.0%   | W4A8=0.0%   | FP=100.0% | Cost=1.000 | Acc=0.6804 | Drop=0.00%
UniformW4A8     W4A4=0.0%   | W4A8=100.0% | FP=0.0%   | Cost=0.500 | Acc=0.6654 | Drop=1.50%
UniformW4A4     W4A4=100.0% | W4A8=0.0%   | FP=0.0%   | Cost=0.250 | Acc=0.5962 | Drop=8.41%
DegreeTopK      W4A4=80.0%  | W4A8=20.0%  | FP=0.0%   | Cost=0.300 | Acc=0.6238 | Drop=5.66%
DegreeErrorTopK W4A4=80.0%  | W4A8=20.0%  | FP=0.0%   | Cost=0.300 | Acc=0.6397 | Drop=4.06%
```

如果后面改了量化实现或默认参数，可以优先看两个 sanity check：

```text
FP16 reference 下 INT4 mean error 是否还在 0.20 左右。
FP16 reference 下 UniformW4A4 Drop 是否还在 8% 左右。
```

如果明显偏离，通常说明 W4A4 pool 不是用当前默认的 `batch_size=64 + w4a_calib_samples=64 + ptq_align_output` 生成的，或者量化代码逻辑发生了变化。
